# Runtime dependencies for the hosted app (Vercel installs from this file).
# The CLI-only packages (typer, rich) are deliberately absent — nothing in the
# web path imports them, and a smaller bundle cold-starts faster.
fastapi>=0.115
httpx>=0.27
pydantic>=2.7
pypdf>=4.2
python-dotenv>=1.0
python-multipart>=0.0.9
PyYAML>=6.0
SQLAlchemy>=2.0
psycopg[binary]>=3.2
cryptography>=42.0
